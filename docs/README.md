# Puurfit Hybrid

This repo contains:
- `site/`      : Vite + Tailwind + Alpine static frontend
- `wp-theme/`  : Minimal WordPress theme skeleton to plug in later

Next steps:
1) Implement Vite config + Tailwind config
2) Build pages in `site/src/pages/`
3) Decide how to sync `site/dist/` into `wp-theme/puurfit/dist/`
